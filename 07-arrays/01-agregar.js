const letras = ['a', 'b'];


// Agregar al final
letras.push('c', 'd')

// Agregar al Inicio  
letras.unshift('1')

// Splice es para agregar elemento entre arreglo
 letras.splice(2, 0, 1, 2)


console.log(letras)